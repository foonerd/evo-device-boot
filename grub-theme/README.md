# grub-theme

Placeholder for an amd64-only GRUB menu theme: brand background and
typography for the boot menu.

Only relevant on multi-boot or developer machines. Reference devices
boot straight through and never show the GRUB menu, so this is
deliberately not part of the v1 install path.

Tracked in `DESIGN.md` under Future work.
